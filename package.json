{
  "name": "pay-sdk-mvp",
  "version": "0.1.0",
  "description": "Embeddable browser JS Pay SDK for Google Pay and Apple Pay",
  "type": "module",
  "types": "dist/types/index.d.ts",
  "scripts": {
    "dev": "vite",
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit && vite build && tsc --emitDeclarationOnly",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/applepayjs": "^14.0.0",
    "@types/googlepay": "^0.7.6",
    "typescript": "^5.5.0",
    "vite": "^5.4.0"
  }
}
